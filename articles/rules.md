# Project Rules

## Package Manager
- Use `bun` for all package management and script execution
- Do NOT use npm, yarn, or pnpm

## Build Tool
- Use `bun` for running scripts and building the project
