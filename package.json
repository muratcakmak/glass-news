{
  "name": "news-data",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "wrangler dev --test-scheduled",
    "setup": "wrangler r2 bucket create news-articles && wrangler kv:namespace create NEWS_KV",
    "deploy:pages": "wrangler pages deploy glass-news --project-name glass-news --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251230.0",
    "@types/bun": "latest",
    "@types/web-push": "^3.6.4",
    "wrangler": "^4.54.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "web-push": "^3.6.7"
  }
}